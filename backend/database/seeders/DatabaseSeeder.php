<?php

namespace Database\Seeders;

use App\Models\Article;
use App\Models\Comment;
use Illuminate\Database\Seeder;

class DatabaseSeeder extends Seeder
{
    public function run(): void
    {
        $articles = [
            [
                'title'   => 'Введение в Laravel: фреймворк для современного PHP',
                'content' => 'Laravel — это элегантный PHP-фреймворк с выразительным синтаксисом. Он берёт на себя рутинные задачи большинства веб-проектов, такие как аутентификация, маршрутизация, сессии и кеширование. Laravel старается сделать разработку приятной, не жертвуя функциональностью. Счастливые разработчики пишут лучший код. С этой целью Laravel попытался объединить лучшее из других веб-фреймворков, включая фреймворки на Ruby, Python и Java. Одной из ключевых особенностей является Eloquent ORM — мощный инструмент для работы с базами данных. Он позволяет описывать связи между моделями, строить сложные запросы и работать с данными как с объектами. Ещё одна сильная сторона — Artisan CLI. Это инструмент командной строки, встроенный в Laravel, который предоставляет множество полезных команд для разработки приложений. Вы можете создавать контроллеры, модели, миграции и многое другое одной командой.',
            ],
            [
                'title'   => 'React Hooks: полное руководство для начинающих',
                'content' => 'React Hooks появились в версии 16.8 и изменили способ написания компонентов. До хуков единственным способом использовать состояние и другие возможности React были классовые компоненты. Хуки позволяют использовать состояние и другие возможности React без написания класса. useState — самый базовый хук. Он позволяет добавить состояние в функциональный компонент. useState принимает начальное значение состояния и возвращает массив из двух элементов: текущее состояние и функцию для его обновления. useEffect — второй по важности хук. Он позволяет выполнять побочные эффекты в функциональных компонентах: запросы к API, подписки, ручное изменение DOM. useEffect принимает функцию, которая будет выполнена после рендеринга компонента. Также можно возвращать функцию очистки, которая будет вызвана перед следующим запуском эффекта или при размонтировании компонента. useContext позволяет читать контекст и подписываться на его изменения. useMemo и useCallback помогают оптимизировать производительность.',
            ],
            [
                'title'   => 'Docker для разработчиков: от нуля до контейнеризации',
                'content' => 'Docker революционизировал способ разработки, доставки и запуска приложений. Контейнеры позволяют упаковать приложение со всеми его зависимостями в стандартизированный блок для разработки программного обеспечения. В отличие от виртуальных машин, контейнеры не включают полную операционную систему, что делает их значительно легче и быстрее. Основные понятия Docker: Image (образ) — это неизменяемый шаблон для создания контейнеров. Container (контейнер) — запущенный экземпляр образа. Dockerfile — текстовый файл с инструкциями для сборки образа. Docker Compose — инструмент для определения и запуска многоконтейнерных приложений. Типичный рабочий процесс с Docker: написание Dockerfile, сборка образа командой docker build, запуск контейнера командой docker run, или использование docker-compose up для многоконтейнерных приложений. Для веб-разработки Docker особенно полезен, так как позволяет создать окружение, идентичное продакшену, прямо на локальной машине.',
            ],
            [
                'title'   => 'REST API: принципы проектирования и лучшие практики',
                'content' => 'REST (Representational State Transfer) — это архитектурный стиль для проектирования сетевых приложений. API, следующие принципам REST, называются RESTful. Ключевые принципы REST: клиент-серверная архитектура, отсутствие состояния (stateless), кешируемость, единообразие интерфейса. Основные методы HTTP в REST: GET используется для получения данных, POST — для создания ресурса, PUT/PATCH — для обновления, DELETE — для удаления. Коды статусов HTTP играют важную роль: 200 OK — успешный запрос, 201 Created — ресурс создан, 400 Bad Request — ошибка в запросе клиента, 404 Not Found — ресурс не найден, 500 Internal Server Error — ошибка сервера. При проектировании REST API важно правильно именовать эндпоинты. Используйте существительные во множественном числе: /articles вместо /getArticles. Версионирование API через URL (/api/v1/) или заголовки помогает управлять изменениями без нарушения обратной совместимости. Документирование API с помощью OpenAPI/Swagger делает его понятным для других разработчиков.',
            ],
            [
                'title'   => 'MySQL: оптимизация запросов и индексирование',
                'content' => 'Производительность базы данных напрямую влияет на скорость работы всего приложения. MySQL предоставляет множество инструментов для оптимизации. Индексы — важнейший инструмент оптимизации. Они ускоряют поиск данных ценой дополнительного дискового пространства и замедления операций записи. Типы индексов в MySQL: PRIMARY KEY — уникальный идентификатор строки, UNIQUE — гарантирует уникальность значений в столбце, INDEX — обычный индекс для ускорения выборки, FULLTEXT — для полнотекстового поиска. Команда EXPLAIN позволяет проанализировать запрос и понять, как MySQL его выполняет: использует ли он индексы, сколько строк сканирует. Это первый инструмент для диагностики медленных запросов. Query Cache (кеш запросов) позволяет MySQL кешировать результаты SELECT-запросов. Однако в MySQL 8.0 он был удалён из-за проблем с масштабируемостью. Правильная нормализация базы данных помогает избежать избыточности данных, однако иногда денормализация оправдана для повышения производительности чтения.',
            ],
        ];

        foreach ($articles as $articleData) {
            $article = Article::create($articleData);

            Comment::create([
                'article_id'  => $article->id,
                'author_name' => 'Иван Иванов',
                'content'     => 'Отличная статья! Очень подробно и понятно написано. Спасибо автору!',
            ]);

            Comment::create([
                'article_id'  => $article->id,
                'author_name' => 'Александра Смирнова',
                'content'     => 'Давно искала такой материал. Буду рекомендовать коллегам.',
            ]);
        }
    }
}
